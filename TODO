-- end-of-video marker? (C0 FF FF)

-- palette change
	-- video start
	-- frame start

-- preprocessing
	-- insert 'whole' frames periodically in stream
	-- brightness/contrast
	-- palette selection
		-- "perfect" palette
		-- palette "flipping" for more virtual colors
			-- RG/GB for virtual 64 colors
			-- RG/GB/RB for virtual 4096 colors?
	-- dithering(?)

-- conversion tools
	-- enforce max data rate
	-- split rle files to comply w/ above
