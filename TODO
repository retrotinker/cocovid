-- performance
	-- reorder code to reduce number of long branches
		
-- palette change (?)
	-- video start
	-- frame start

-- preprocessing
	-- use RGB lookup table for selecting colors instead of algorithm
	-- sort drl segments by total color distance rather than length of run
	-- insert 'whole' frames periodically in stream
	-- brightness/contrast
	-- palette selection (?)
		-- "perfect" palette
		-- palette "flipping" for more virtual colors
			-- RG/GB for virtual 64 colors
			-- RG/GB/RB for virtual 4096 colors?
		-- Chris Lomont's single palette 2x+y scheme?
	-- dithering (?)
	-- scoring based strictly on size of input + output bitstreams
	-- use fewer tmp files (i.e. more piping) to improve conversion time

-- limit length of runs in drlprune, splitting as necessary
