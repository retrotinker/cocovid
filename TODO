-- frame-by-frame compression (C0 XX YY as marker)

-- start-of-frame marker
	-- C0 00 00
	-- could enable variable data at end of frame
		-- audio
		-- palette

-- end-of-video marker (C0 FF FF)

-- palette change
	-- video start
	-- frame start

-- preprocessing
	-- insert 'whole' frames periodically in stream
	-- brightness/contrast
	-- palette selection
		-- "perfect" palette
		-- palette "flipping" for more virtual colors
			-- RG/GB for virtual 64 colors
			-- RG/GB/RB for virtual 4096 colors?
	-- dithering(?)
