-- player
	-- frame number marker (?)
	-- display message (possibly frame number) while paused
	-- video header to select parameters (colors, fps, etc)
	-- encode video length in header (enable skipping video) (?)
	   (using a directory gives same function)
	-- read fewer extra sectors at a time (maybe none)
	-- TC^3 SCSI driver (and driver/filesystem API?)
	-- macros (or overlays) for multi-driver builds
	-- reenable interrupts (for both video and audio?)
	-- drop excessive frames

-- integrated video conversion utility to process ppm/pcm data (?)
	-- dithering optional
	-- variable bitrate
	-- select resolution
	-- alternatively, clean-up buildout
		-- better use of mktemp for temporary files/directories
	-- account for pixel aspect ratio (CMP 10:11?, RGB 1:1? or 5:6?)
	-- use ffmpeg and/or ImageMagick libraries instead of exec'ing utilities
	-- re-implement toolchain as codec and container support for ffmpeg
		-- http://wiki.multimedia.cx/index.php?title=FFmpeg_codec_howto
		-- http://www.inb.uni-luebeck.de/~boehme/using_libavcodec.html

-- misc
	-- capture actual composite palette examples and pick better(?)
	   equivalents for RGB palette

-- applications
	-- "laser disc"-style game
	-- vnc-like remote desktop
	-- DVD "emulator" (i.e. rip/translate DVD menus as well as A/V)
